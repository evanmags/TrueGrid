const gds=document.querySelectorAll('.grid'),genG=()=>gds.forEach(g=>{let columns=g.getAttribute('columns');let rows=g.getAttribute('rows');g.style.gridTemplateColumns=columns;g.style.gridTemplateRows=rows});window.addEventListener('load',genG)